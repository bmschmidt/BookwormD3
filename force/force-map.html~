    <!DOCTYPE html>
    <html>
    <head>
    <title>Force-Directed Map</title>
    <script type="text/javascript" src="../../d3.v2.js"></script>
    <style type="text/css">
<<<<<<< HEAD

=======
    
>>>>>>> 15e4f41a52ce0c8164cf9fec4f6888f9f68d351b
path {
    fill: #ddd;
    fill-opacity: .8;
    stroke: #fff;
    stroke-width: 1.5px;
}

line {
    stroke: #999;
}

</style>
    </head>
    <body>
    <script type="text/javascript">
<<<<<<< HEAD

var width = 960,
 height = 500;
=======
    
var width = 960,
height = 500;
>>>>>>> 15e4f41a52ce0c8164cf9fec4f6888f9f68d351b

var path = d3.geo.path(),
force = d3.layout.force().size([width, height]);

var svg = d3.select("body").append("svg")
    .attr("width", width)
    .attr("height", height);
<<<<<<< HEAD
var node
var nodes = []
var links;


d3.json("../data/us-states.json", function(states) {
    nodes = []
    links = [];

    states.features.forEach(function(d, i) {
        // A few states are at fake coordinates
        if (d.id == "AK" || d.id == "PR" || d.id == "HI") return;
        var centroid = path.centroid(d);
        centroid.x = centroid[0];
        centroid.y = centroid[1];
        centroid.feature = d;
        nodes.push(centroid);
    });

    d3.geom.delaunay(nodes).forEach(function(d) {
        links.push(edge(d[2],d[1]))
        links.push(edge(d[0],d[1]))
        links.push(edge(d[0],d[2]))
    })
    links = []

    nodes.map(function(d) {
        closestStates = []
        nodes.map(function(e) {
            link = edge(d,e)
            if (link.distance <= 750) {
                if (link.distance > 0) {
                    closestStates.push(link)
                }
            }
        })
	closestStates.sort(function(a,b) {return(b.distance-a.distance)})
	closestStates.slice(-2).map(function(d) {links.push(d)})
    })

    force
        .gravity(0.05)
        .charge(-10)
        .nodes(nodes)
        .links(links)
        .linkDistance(function(d) { return d.distance*1.3; })
        .start();

    var link = svg.selectAll("line")
        .data(links)
        .enter().append("line")
        .attr("x1", function(d) { return d.source.x; })
        .attr("y1", function(d) { return d.source.y; })
        .attr("x2", function(d) { return d.target.x; })
        .attr("y2", function(d) { return d.target.y; });

    node = svg.selectAll("g")
        .data(nodes)
        .enter().append("g")
        .attr("transform", function(d) {
            return "translate("  + -d.x + "," + -d.y + ")";
        })
        .call(force.drag)
        .append("path")
        .attr("transform", function(d) { return "translate(" + d.x + "," + d.y + ")" })
        .attr("d", function(d) { return path(d.feature); })


    force.on("tick", function(e) {
        node.attr("transform", function(d) {
            return "translate(" + d.x + "," + d.y + ")";
        });
	
        link.attr("x1", function(d) { return d.source.x; })
=======

d3.json("../data/us-states.json", function(states) {
    var nodes = [],
    realcents = new Array(),
    links = [];
    
    states.features.forEach(function(d, i) {
	// A few states are at fake coordinates
	if (d.id == "AK" || d.id == "PR" || d.id == "HI") return;
	
	var centroid = path.centroid(d);
	centroid.x = centroid[0];
	centroid.y = centroid[1];
	centroid.yrand = Math.random() *700
	centroid.xrand = Math.random() *700
	centroid.xorig = centroid[0];
	centroid.yorig = centroid[1];
	//centroid.x = Math.random()*750 *1;
	//centroid.y = Math.random()*750 *1;
	centroid.feature = d;
	nodes.push(centroid);
    });

    d3.geom.delaunay(nodes).forEach(function(d) {
	links.push(edge(d[2],d[1]))
	links.push(edge(d[0],d[1]))
	links.push(edge(d[0],d[2]))
    });
//    var newnodes = [];
//    d3.selectAll(nodes).forEach(function(d) {
//	d.x = d.xrand;
//	d.y = d.yrand;
//	nodes.push(d);
//    } );
//    nodes = newnodes;

    force
	.gravity(0)
	.charge(0)
	.nodes(nodes)
	.links(links)
	.linkDistance(function(d) { return d.distance; })
        .start();
    
    var link = svg.selectAll("line")
	.data(links)
	.enter().append("line")
	.attr("x1", function(d) { return d.source.xrand; })
	.attr("y1", function(d) { return d.source.yrand; })
	.attr("x2", function(d) { return d.target.xrand; })
	.attr("y2", function(d) { return d.target.yrand; });
    
    var node = svg.selectAll("g")
	.data(nodes)
	.enter().append("g")
        .attr("transform", function(d) {
	    return "translate("  + -d.x + "," + -d.y + ")";
	})
    
	.call(force.drag)
	.append("path")
	.attr("transform", function(d) { return "translate(" + d.xrand + "," + d.yrand + ")"; })
	.attr("d", function(d) { return path(d.feature); });
    
    force.on("tick", function(e) {
//	node.attr("transform", function(d) {
//	    return "translate(" + d.x + "," + d.y + ")";
//	});
	
	link.attr("x1", function(d) { return d.source.x; })
>>>>>>> 15e4f41a52ce0c8164cf9fec4f6888f9f68d351b
            .attr("y1", function(d) { return d.source.y; })
            .attr("x2", function(d) { return d.target.x; })
            .attr("y2", function(d) { return d.target.y; });
    })
<<<<<<< HEAD
        .linkDistance(function(d) {return d.distance})
    muss(1)
    //nodes.map(function(d) {
//	if (d.x < 0) {d.x=0}
//	if (d.y < 0) {d.y=0}
//	if (d.x > width) {d.x = width}
//	if (d.y > height) {d.x = height}
  //  }
=======
    .linkDistance(function(d) {return d.distance})
>>>>>>> 15e4f41a52ce0c8164cf9fec4f6888f9f68d351b
});

function edge(a, b) {
    var dx = a.x - b.x, dy = a.y - b.y;
    return {
<<<<<<< HEAD
        source: a,
        target: b,
        distance: Math.sqrt(dx * dx + dy * dy)
    };
}

function muss(fraction) {
    nodes.map(function(n) {
	console.log(n.feature.id);
	if (Math.random() < fraction) {
            n.x = Math.random()*width;
            n.y = height*Math.random();
	}
	return(n)
    })
    force.update()
}
	  


</script>
    </body>
    </html>
=======
	source: a,
	target: b,
	distance: Math.sqrt(dx * dx + dy * dy)
    };
}

    </script>
  </body>
</html>
>>>>>>> 15e4f41a52ce0c8164cf9fec4f6888f9f68d351b
